# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup

# Secrets — encrypted .yaml files are safe to commit but we ignore by default
# to prevent accidental plaintext commits. Track only templates and docs.
secrets/
!secrets/README.md
!secrets/**/*.template
*.age

# Nix
result

# Old configs (contain their own .git, not submodules)
pluto-config/
old-config/
