# Management secrets — threshold=2 (YubiKey + pluto host key required to decrypt).
# Example: GitHub tokens, DNS API keys, shared bootstrap secrets.
#
# This is a plaintext template. Encrypt with:
#   sops --encrypt --in-place secrets/management/bootstrap.yaml
#
# Before encrypting, fill in .sops.yaml placeholders with real age public keys.
example_token: "PLAINTEXT_TEMPLATE_DO_NOT_COMMIT"
